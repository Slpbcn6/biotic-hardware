.pytest_cache/
__pycache__/
*.pyc

# All generated pipeline artifacts (reproducible) go to outputs/ and are not committed.
outputs/*
!outputs/.gitkeep

# The sensitivity plot is also written to data/sensitivity_analysis.png and IS
# committed on purpose, because the repository OVERVIEW links to it there.